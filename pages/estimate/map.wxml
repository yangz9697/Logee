<view class="container">
  <map 
    id="map"
    class="map"
    longitude="{{longitude}}"
    latitude="{{latitude}}"
    scale="{{scale}}"
    markers="{{markers}}"
    polyline="{{polyline}}"
    include-points="{{includePoints}}"
    show-location
  ></map>
  <view class="info-panel">
    <view class="route-info">
      <text class="distance">总距离：{{distance}}</text>
      <text class="duration">预计时间：{{duration}}</text>
    </view>
    <view class="address-info">
      <view class="start">
        <text class="label">起点：</text>
        <text class="value">{{startAddress}}</text>
      </view>
      <view class="end">
        <text class="label">终点：</text>
        <text class="value">{{endAddress}}</text>
      </view>
    </view>
  </view>
</view> 